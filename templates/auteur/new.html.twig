{% extends 'base.html.twig' %}

{% block title %}New Auteur{% endblock %}

{% block body %}
<div class="col-md-6 offset-md-3 mt-5">
    <div class="card">
              <div class="card-header text-white bg-dark mb-3">Ajouter un nouveau Auteur</div>
              <div class="card-body">
                {{ include('auteur/_form.html.twig') }}
              </div>
      </div>
                  <div class="row mt-3">
                          <div class="col-md-6 offset-md-3">
                            <a href="{{ path('auteur_index') }}" class="btn btn-primary"><i class="bi bi-arrow-left-circle"></i> Retour à la liste des auteurs</a>
                          </div>
                 </div>
</div>
{% endblock %}
