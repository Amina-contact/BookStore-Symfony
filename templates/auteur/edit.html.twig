{% extends 'base.html.twig' %}

{% block title %}Edit Auteur{% endblock %}

{% block body %}

<div class="col-md-6 offset-md-3 mt-4">
            <div class="card">
                <div class="card-header text-white bg-dark mb-2">Modifier Auteur</div>
                <div class="card-body">
                    {{ include('auteur/_form.html.twig', {'button_label': ' Update'}) }}
                </div>
                <div class="row mt-3 mb-2">
                        <div style=" display: flex;justify-content: center;">
                            <a href="{{ path('auteur_index') }}" class="btn btn-primary" style="margin-right:10px"><i class="bi bi-arrow-left-circle"></i> Retour</a>   {{ include('auteur/_delete_form.html.twig') }}</div>           
                        </div>
                </div>
            </div>
</div>
{% endblock %}
