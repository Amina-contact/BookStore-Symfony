{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>

            </tr>
            <tr>

            </tr>
        </tbody>
    </table>
<div class="container mt-5 col-md-6 col-md-offset-3">
        <div class="card text-center">
                <div class="card-header text-white bg-dark mb-3">
                        <h2>Genre Details</h2>
                </div>
                <div class="card-body ">
                    <div class="row justify-content-center" style="display: flex;justify-content: center;">
                           <h3>Nom : {{ genre.nom }}</h3>
                            <div class="col-1 col-md-2 mb-2">ID :  {{ genre.id }}</div>
                            <!-- Force next columns to break to new line -->
                            <div class="w-100"></div>
                </div>
        </div>
        <div class="card-footer text-muted text-white bg-dark">
           <div style=" display: flex;justify-content: center;">
                    {% if is_granted('ROLE_ADMIN') %}
							   <a href="{{ path('genre_edit', {'id': genre.id}) }}" class="btn btn-success" style="margin-right:10px"><i class="bi bi-pencil-square"></i> Modifier</a>   {{ include('genre/_delete_form.html.twig') }} 
					{% endif %}
            </div>                
        </div>
</div>
            <div class="row mt-3">
                <div  style="display: flex;justify-content: center;">
                    <a href="{{ path('genre_index') }}" class="btn btn-primary"><i class="bi bi-arrow-left-circle"></i> Retour à la liste des genres</a>
                </div>
            </div>
{% endblock %}
