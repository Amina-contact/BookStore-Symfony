{% extends 'base.html.twig' %}

{% block title %}New User{% endblock %}

{% block body %}
<div class="col-md-6 offset-md-3 mt-5">
<div class="card" style="max-width: 30rem;">
  <div class="card-header text-dark bg-warning mb-3">Create new User</div>
  <div class="card-body">
      {{ include('user/_form.html.twig') }}
  </div>
</div>
<div class="row mt-3">
     <div style=" display: flex;margin-left:95px">
        <a href="{{ path('user_index') }}" class="btn btn-warning"><i class="bi bi-arrow-left-circle"></i> Retour à la liste des utilisateurs</a>
     </div>
</div>
</div>
{% endblock %}
